node_modules
.DS_Store
src/data/*.json.bak

# Generated build output — the live site is served from the gh-pages branch,
# so there's no reason to track this on main (it just drifts on every deploy).
/build